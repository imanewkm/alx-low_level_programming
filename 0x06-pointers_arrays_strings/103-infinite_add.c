#include "main.h"
/**
 * infinite_add - addition
 * @n1: input
 * @n2: input
 * @r: buffer
 * @size_r: size
 * Return: 0
 */
char *infinite_add(char *n1, char *n2, char *r, int size_r)
{
	r = n1 + n2;
	size_r = sizeof(r);
}
